enum StatusType { init, loading, success, error }

enum MediaStatus { init, start, pause, complete, stop, error }
enum AutoScrollStatus { init, start, pause, complete, stop, error }


enum PanelStatus { hide, show }

enum PanelType { hide, base, autoScroll, media }

enum ReadBookType { base, autoScroll, media }
